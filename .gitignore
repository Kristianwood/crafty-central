# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — .env holds the real credentials and is never committed.
# Only .env.example belongs in the repo.
.env
.env.*
!.env.example

# written into the app directory on a deployed server by the Command Center
build-info.json
/.forthway/

# runtime
/logs
/public/uploads
tsconfig.tsbuildinfo
