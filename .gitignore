# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env — secrets live on the server and survive every deploy
.env
.env*.local

# written into the app directory on a deployed server by the Command Center
build-info.json
/.forthway/

# runtime
/logs
/public/uploads
tsconfig.tsbuildinfo
